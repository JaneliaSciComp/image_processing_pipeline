class Settings:
   defaultFileBase = '<path>/Lightsheet-Processing-Pipeline/Compiled_Functions/sampleInput_'
   outputDirectoryBase = '<path>/interface_output/'
   lightsheetApi = '<api_url>/api/rest-v2/services/'
   mongo = <host:port of mongo instance>
   username = <lightsheet username>
   serverInfo = {'IP': <server IP address>, 'port' : <server port number>}
   serverInfo['fullAddress'] = 'http://' + serverInfo['IP'] + ':' + str(serverInfo['port']) + '/'
   devOrProductionJACS =  '<api_url>/api/rest-v2/services/'	
